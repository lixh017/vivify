// Re-export the API client surface. Allows `@opc/shared/api`
// to resolve to this directory's entry (TypeScript prefers
// `index.ts` over named files when resolving a path mapping
// to a directory).

export * from './client'
